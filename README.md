# tanstack-start-rsc-skill

Public distribution repo for the `tanstack-start-rsc` Codex-compatible skill.

This repository contains:

- `SKILL.md` with the main skill instructions
- `docs/` with supporting guidance
- `examples/` with copy-pasteable patterns

The full README is temporary for now and will be replaced.
